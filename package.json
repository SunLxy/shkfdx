{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna exec --scope @cstu-react/* -- npm run build",
    "watch": "npm run-script watch --workspace @cstu-react/pro",
    "dev": "npm run-script dev --workspace example",
    "version": "lerna version --exact --force-publish --no-push --no-git-tag-version"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2",
    "tsbb": "4.2.5",
    "typescript": "5.8.2"
  }
}